// src/styles/theme.ts
export const theme = {
    colors: {
        background: '#213448', // ✅ 어두운 남색 (원하는 코드로 바꿀 수 있음)
        text: '#ffffff',       // 기본 글씨 색 (선택)
    }
}

export type ThemeType = typeof theme